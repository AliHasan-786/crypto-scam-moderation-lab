# Reviewer-Assistant Red Team

The fixture treats post text as hostile input. It includes instruction override,
schema spoofing, fabricated-evidence prompts, policy-quote spoofing, Unicode
controls, and roleplay framing. The runner currently tests only the deterministic
extractor; it is not evidence of hosted-model robustness.
