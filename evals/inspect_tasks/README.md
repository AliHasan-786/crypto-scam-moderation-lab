# Inspect Packaging

`inspect_ai` is an optional dependency and is not installed in this environment.
The task wrapper exposes frozen local scenario cases after installation:

```bash
pip install inspect-ai
inspect eval evals/inspect_tasks/scenario.py
```
